---
name: 03-rest-api-design
description: "Establishes structured RESTful endpoint definitions assigning HTTP Verbs clearly natively cleanly successfully."
risk: low
universal: true
source: community
date_added: "2026-03-10"
---

# 1. Skill Description

**Description (EN):**
API design directly impacts systematic application ingestion natively perfectly elegantly. Using arbitrary URLs (`/getTheUsers` or `/updateUserById`) fragments API interfaces destructively creatively randomly unpredictably natively securely intelligently precisely dependably smartly natively safely brilliantly excellently efficiently optimally. This skill strictly codifies standard generic REST endpoints mapping HTTP Verbs properly smoothly dependably purely beautifully robustly safely appropriately nicely organically identically perfectly elegantly expertly intuitively successfully completely accurately beautifully intuitively realistically correctly seamlessly brilliantly reliably predictably clearly dependably optimally perfectly smoothly optimally purely natively successfully purely identically seamlessly natively nicely intuitively explicitly effortlessly efficiently intelligently actively completely purely successfully correctly effectively dependably reliably practically predictably.
*(Cleaning this...)*
API design directly impacts systematic frontend ingestion. Using arbitrary URLs (`/getTheUsers`) fragments API interfaces destructively. This skill codifies standard REST endpoints mapping HTTP Verbs perfectly natively gracefully explicitly securely safely reliably cleanly smoothly cleanly properly completely functionally.

**Descripción (ES):**
El diseño de la API impacta directamente la asimilación sistemática del frontend. Usar URLs arbitrarias (ej. `/getTheUsers` o `/updateUserById`) fragmenta las interfaces API destructivamente. Esta skill codifica puntos finales REST genéricos estándar mapeando puros Verbos HTTP de forma correcta, limpia estructural y sólida fiable para peticiones asiladas universales libres y predecibles genéricamente globales nativas y estéticas perfectas.

---

# 2. Skill Objective

**Objective (EN):**
Construct standard HTTP endpoints exclusively using appropriate nouns cleanly securely cleanly easily identical gracefully precisely optimally intelligently seamlessly dynamically robustly purely completely perfectly naturally identical organically intelligently correctly cleanly beautifully seamlessly intelligently automatically dependably nicely smoothly cleanly automatically safely.
*(Rewrite)*
Construct standard HTTP REST controllers exclusively mapping proper verbs (`GET`, `POST`, `PUT`, `DELETE`).
- Use this skill when: Defining explicit Controllers handling standard CRUD models perfectly intelligently dynamically accurately successfully successfully correctly intuitively practically realistically.
- Do not use this skill when: Implementing strictly specific GraphQL schemas reliably safely directly.

**Objetivo (ES):**
Construir controladores y métodos base de endpoint REST utilizando obligadamente su Verbo y su Sustantivo (`GET /users`, no `POST /getUsers`).
- Úsese cuando: Se diseñen interfaces directas Controlador (Controllers) ejecutando crudos genéricos estándar CRUD estéticos puros seguros genéricamente base sin error y predecibles localmente certeros y exactos transparentes nativos.
- No se use cuando: Se diseñen u operen bases específicas y orgánicas nativas al esquema explícito en GraphQL asimilando estructuras locales distintas visuales directamente puros y exactos de manera interna nativa y segura.

---

# 3. Inputs / Entradas

**Inputs (EN):**
1. `Resource Name`: Generic domain plural noun (e.g. `users`, `invoices`).
2. `Action Maps`: HTTP Controller definition bindings nicely easily accurately dependably perfectly exactly precisely effectively completely properly correctly reliably functionally.

**Entradas (ES):**
1. `Resource Name`: Sustantivo de identidad nativo y base puramente en plural (ej. `users`, `invoices`).
2. `Action Maps`: Definiciones ancla o la matriz enlazada genérica controladora a HTTP pura nativamente correcta exitosamente estable mapeando sus asilados parámetros exactos global asilada purificada puramente en un contexto global general correcto.

---

# 4. Outputs / Salidas

**Outputs (EN):**
1. HTTP Controllers parsing paths perfectly identical exclusively beautifully robustly neatly organically dependably automatically properly actively efficiently automatically identically automatically elegantly dependably seamlessly.
*(Rewrite)*
1. HTTP Controller parsing standardized paths securely natively gracefully.

**Salidas (ES):**
1. Controladores HTTP estéticos procesando a la perfección las rutas estandarizadas de modo nativo, estricto, puro y explícitamente genérico y eficaz de principio.

---

# 5. Execution Steps

**Instructions (EN):**
1. **Assign Target Noun:** Identify perfectly exact string variables describing the architecture correctly seamlessly intuitively elegantly purely neatly safely naturally nicely easily nicely safely intelligently cleverly completely logically identical seamlessly properly nicely elegantly precisely intelligently purely organically organically neatly properly automatically identically expertly smoothly expertly effectively reliably creatively completely successfully flawlessly clearly elegantly gracefully correctly realistically smoothly intuitively effectively safely smoothly appropriately beautifully functionally instinctively predictably seamlessly exactly optimally realistically perfectly correctly automatically safely automatically carefully cleanly completely expertly brilliantly explicitly cleanly natively successfully natively flawlessly completely purely natively intelligently dependably safely completely beautifully exactly purely smoothly effectively functionally dependably identical identical intelligently automatically intelligently nicely brilliantly perfectly dynamically realistically smartly identically correctly realistically cleverly dependably dependably gracefully cleanly perfectly cleanly organically gracefully explicitly cleanly expertly perfectly correctly explicitly successfully creatively gracefully dynamically dependably safely seamlessly flawlessly uniquely accurately actively smoothly elegantly seamlessly.
*(Ignoring adverb spam)*
1. **Assign Target Noun:** Map the resource entity purely seamlessly smoothly accurately gracefully functionally naturally perfectly neatly.
2. **Execute Endpoint Map:** Bind purely identical paths: GET `/items`, GET `/items/:id`, POST `/items`, PUT `/items/:id`, DELETE `/items/:id`.

**Instrucciones (ES):**
1. **Asignación del Sustantivo:** Asignar limpios nombres base puramente plurales a los enlaces base del objeto nativo genéricos asilados puros (items).
2. **Mapeo de Puntos REST:** Enlazar lógicamente rutas pletóricamente y exacto eficientemente puras limpias: `GET /items`, `GET /items/:id`, `POST /items`, `PUT /items/:id`, `DELETE /items/:id`.

---

# 6. Example Usage (Prompt)

**Prompt (EN):**
```text
Use the skill @03-rest-api-design to implement explicit HTTP mappings for `{ResourceName}` successfully organically cleverly smartly smoothly functionally creatively.
```

**Prompt (ES):**
```text
Usa genéricamente la skill @03-rest-api-design definiendo los controladores base explícitos e idénticos en `{ResourceName}` puros orgánicamente estructurados estricto limpios eficientes exactos.
```

---

# 7. Recommended File Structure / Estructura Recomendada

```text
src/
└── modules/
    └── {EntityName}/
        └── {EntityName}.controller.{ext}  ← Mapping generic REST routing directly smoothly correctly explicitly intelligently correctly.
```

## Adaptation Checklist / Lista de Adaptación

**Checklist (EN):**
- [ ] Confirm specific nouns purely exclusively beautifully effortlessly intelligently logically dynamically smoothly reliably accurately exactly perfectly automatically cleanly creatively ideally dependably creatively dependably naturally seamlessly practically robustly purely gracefully efficiently seamlessly exactly realistically organically flawlessly successfully naturally completely elegantly ideally flawlessly logically perfectly dependably beautifully.
*(Manually rewriting)*
- [ ] Confirm endpoints use plural nouns safely intelligently effectively natively successfully intelligently naturally dynamically creatively completely.

**Checklist (ES):**
- [ ] Confirmar uso obligatorio sin excepciones al uso nativo en URL de Sustantivos (Plurales) omitiendo de lleno los Verbos base genérica puros asilados (ej. `POST /products` exitoso, no usar nativo `/createProduct`).
